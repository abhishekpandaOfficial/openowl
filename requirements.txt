# ── Core Framework ──────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0

# ── LangGraph (Agent Orchestration) ─────────────────────────
langgraph==0.2.50
langchain-core==0.3.20
langchain-community==0.3.7

# ── AI Model SDKs ────────────────────────────────────────────
groq==0.11.0                    # Groq API (Mistral/LLaMA, free tier)
anthropic==0.36.0               # Claude (fallback 1)
openai==1.54.0                  # GPT-4o-mini (fallback 2)
httpx==0.27.2                   # Ollama + general HTTP (async)

# ── Telegram Bot ─────────────────────────────────────────────
python-telegram-bot[webhooks]==21.6

# ── Twilio (WhatsApp + SMS + Calls) ──────────────────────────
twilio==9.3.5

# ── Database & Memory ────────────────────────────────────────
asyncpg==0.30.0                         # PostgreSQL async driver
sqlalchemy[asyncio]>=2.0.30,<2.0.36    # ORM + async support
redis[asyncio]==5.2.0                   # Redis async client
alembic==1.14.0                          # Database migrations

# ── Voice (optional — remove if not using phone calls) ───────
# openai-whisper==20240930      # Local Whisper STT (uncomment to enable)
# pydub==0.25.1                 # Audio processing

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.1
python-multipart==0.0.12        # Form data parsing (Twilio webhooks)
aiofiles==24.1.0

# ── Development ──────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2                   # Test client

# ── Phase 2 additions ──────────────────────────────────
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.151.0
apscheduler==3.10.4
pydub==0.25.1
tzdata==2024.2
